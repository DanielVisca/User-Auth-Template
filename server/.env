# Copy to .env and set in production.

# Required in production: use a long random secret (e.g. openssl rand -hex 32)
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32

# Database: default SQLite. For production use PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://user:pass@host/dbname
# DATABASE_URL=sqlite+aiosqlite:///./auth.db

# Cookie: set to true when using HTTPS
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax

# CORS: frontend origin(s), comma-separated
# CORS_ORIGINS=http://localhost:5173,https://yourapp.com

# Optional: email (if not set, reset/verify links are logged to console)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# MAIL_FROM=noreply@example.com

# Frontend URL for reset/verify links in emails
FRONTEND_URL=http://localhost:5173
