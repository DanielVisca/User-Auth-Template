# Client (React). Only VITE_* are exposed to the browser.
# Development: leave unset to use Vite proxy (/api -> backend).
# Production: set to your backend URL, e.g. https://api.example.com
# VITE_API_URL=
